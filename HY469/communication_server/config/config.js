module.exports = {

    remoteMessengerPort: 8123, // the port of the socket where the clients (Web Pages) will connect

    restServerPort : 8760, // the port where the clients (Web Pages) will make the remote calls

    sensorSocketPort : 7684, // the port where the kinect and phidget sensors will connect.

    hololensPort : 4567,

};